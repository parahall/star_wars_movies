apply plugin: 'com.android.application'

android {
  compileSdkVersion 26
  buildToolsVersion "26.0.1"
  defaultConfig {
    applicationId "com.academy.android.starwarsmovies"
    minSdkVersion 16
    targetSdkVersion 26
    versionCode 1
    versionName "1.0"
    testInstrumentationRunner "android.support.test.runner.AndroidJUnitRunner"
  }
  buildTypes {
    release {
      minifyEnabled false
      proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
    }
  }
}

dependencies {
  implementation 'com.android.support:design:26.0.0'
  compile fileTree(dir: 'libs', include: ['*.jar'])
  androidTestCompile('com.android.support.test.espresso:espresso-core:2.2.2', {
    exclude group: 'com.android.support', module: 'support-annotations'
  })

  ext {
    flatfoot = "1.0.0-alpha7"
    support_lib = "26.0.0"
    glide = "3.7.0"
    butter_knife = "8.6.0"
    gson = "2.8.0"
    junit = "4.12"
    dagger = "2.10"
    firebase = "11.0.4"
  }

  compile "com.android.support:appcompat-v7:${support_lib}"
  compile "com.github.bumptech.glide:glide:${glide}"
  compile "com.jakewharton:butterknife:${butter_knife}"
  annotationProcessor "com.jakewharton:butterknife-compiler:${butter_knife}"
  compile "com.google.code.gson:gson:${gson}"
  testCompile "junit:junit:${junit}"
  compile "com.google.firebase:firebase-auth:${firebase}"
  compile "com.google.android.gms:play-services-auth:${firebase}"
  compile "com.google.firebase:firebase-database:${firebase}"
  compile "com.google.firebase:firebase-config:${firebase}"
  //Flatfoot
  compile "android.arch.lifecycle:runtime:${flatfoot}"
  compile "android.arch.lifecycle:extensions:${flatfoot}"
  annotationProcessor "android.arch.lifecycle:compiler:${flatfoot}"
  compile "android.arch.persistence.room:runtime:${flatfoot}"
  annotationProcessor "android.arch.persistence.room:compiler:${flatfoot}"
  compile "com.google.dagger:dagger:${dagger}"
  annotationProcessor "com.google.dagger:dagger-compiler:${dagger}"
}



apply plugin: 'com.google.gms.google-services'